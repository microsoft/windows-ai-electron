{
  "name": "test-app",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "start": "electron --no-sandbox .",
    "setup-debug": "npx winapp node add-electron-debug-identity",
    "postinstall": "npx winapp init && npm run setup-debug"
  },
  "devDependencies": {
    "electron": "^39.0.0"
  },
  "dependencies": {
    "@microsoft/winappcli": "^0.1.7"
  }
}
